use Mix.Config

config :server, :general,
  domain: 'example.com',
  client_db_filename: 'client_db',
  script_db_filename: 'script_result_db',
  auto_save: 60 * 1000,
  crawl_interval: 3000,
  cert_path: "/path/to/cert",
  key_path: "/path/to/key"

config :server, :network,
  client_port: 10101,
  api_port: 10102,
  timeout: 1000
